import machine

led = machine.Pin('LED', machine.Pin.OUT)
button = machine.Pin(17, machine.Pin.IN, machine.Pin.PULL_DOWN)

while True:
    if button.value():
        led.value(True)
    else:
        led.value(False)
